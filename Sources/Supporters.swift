import Foundation

/// 앱 정보(About) 창에 이름을 올리는 후원자 목록.
///
/// **이름 공개에 동의한 사람만** 넣는다. GitHub Sponsors·Buy Me a Coffee·후원자 키 결제 중
/// 어디서 후원했든 같다. 릴리스 전에 추가하면 다음 버전부터 보인다.
/// 비어 있으면 About 창에 후원자 목록 자리가 나오지 않는다.
enum Supporters {
    /// 표시 이름. 후원자가 원하는 이름(실명·닉네임·GitHub 아이디) 그대로, 후원한 순서대로.
    static let names: [String] = []
}
